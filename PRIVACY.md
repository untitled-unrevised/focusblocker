# Privacy

FocusBlocker does not include analytics, advertising, telemetry, or a remote
service. It does not transmit browsing history or the configured block list.

The extension stores the domains you add in the browser's local extension
storage. Removing the extension normally removes that storage according to the
browser's extension-data behavior.

The source repository contains no default block list or personal configuration.
